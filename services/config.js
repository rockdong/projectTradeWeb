const config = {
    db: {
        sqlite: 'sqlite:/Users/user/workspace/db/TradeProject.db',
        offset: 10,
        limit: 10
    },
    version: 'v1',
    jwt_secret: 'trade_project'
}

module.exports = config